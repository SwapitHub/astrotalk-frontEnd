// next.config.mjs
/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["astro.weddingbyte.com", "localhost"], // development ke liye
  },
};

export default nextConfig;
